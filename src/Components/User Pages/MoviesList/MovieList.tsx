import { FC } from 'react';
import ViewAllPage from '../../Common Component/ViewAllPage/ViewAllPage';

interface IProps {}

const MovieList: FC<IProps> = (props: IProps) => {
  return (
    <>
      <ViewAllPage />
    </>
  );
};

export default MovieList;
